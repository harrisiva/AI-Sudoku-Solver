def backtrack():
    return

def backtracking_serach():
    # Select-Unassigned-Variable will use the minimum remaning values (MRV) heuristic (ones with the smallest domain lengths)
    # Order-Domain-Values (the order at which domain values are tried, irrelevant? just use domain as is)    
    # Or we could use the least_constraining_value (one that leaves the most number of blanks open?)

    return